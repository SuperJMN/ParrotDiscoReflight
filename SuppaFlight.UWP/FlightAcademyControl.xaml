<UserControl
    x:Class="SuppaFlight.UWP.FlightAcademyControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SuppaFlight.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:flightAcademy="using:FlightVisualizer.Core.FlightAcademy"
    xmlns:code="using:SuppaFlight.UWP.Code"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" d:DataContext="{d:DesignInstance code:MainViewModel}">

    <StackPanel>
        <Button Command="{Binding LoadFlightsCommand}" HorizontalAlignment="Right"
                Content="Load" />
        <ListView MaxHeight="400" ItemsSource="{Binding Flights}"
                  SelectedItem="{Binding SelectedFlight, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="flightAcademy:Flight">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Date}" />
                        <TextBlock Text="{Binding TotalRunTime}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</UserControl>
