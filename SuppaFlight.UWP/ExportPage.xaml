<Page
    x:Class="SuppaFlight.UWP.ExportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SuppaFlight.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:code="using:SuppaFlight.UWP.Code"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" d:DataContext="{d:DesignInstance code:ExportViewModel}">
    <Grid>
        <ContentControl VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" x:Name="RenderControl">
            <Grid>
                <local:BatteryControl Width="32" VerticalAlignment="Top" Margin="8" HorizontalAlignment="Left" />
                <local:StatusControl VerticalAlignment="Bottom" />
            </Grid>
        </ContentControl>
        <ProgressRing VerticalAlignment="Center" HorizontalAlignment="Center" IsActive="{Binding IsBusy}" Height="100" Width="100" Foreground="Red" />
    </Grid>
</Page>
